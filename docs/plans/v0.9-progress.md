# Entangle UI v0.9 — Progress & Handoff

Working branch: `claude/entangle-ui-v0.9-release-iT5GO`
Each phase from 2.x onwards is delivered in its own session for focused review.

## Done in session 1 (Phase 1 + 2.1 + 2.2)

### Phase 1 — Hooks (5/5)

All committed, tested, type-checked, and exported from `entangle-ui`.

| Hook                                         | Files                                | Tests | Notes                                                                                   |
| -------------------------------------------- | ------------------------------------ | ----- | --------------------------------------------------------------------------------------- |
| `useMediaQuery`                              | `src/hooks/useMediaQuery/`           | 5     | SSR-safe, modern + legacy listener API                                                  |
| `useBreakpoint`                              | `src/hooks/useBreakpoint/`           | 3     | Composes `useMediaQuery`. Exposes `breakpoints` token (576/768/992/1200) from `@/theme` |
| `useDebouncedValue` + `useDebouncedCallback` | `src/hooks/useDebounced/`            | 12    | Both in one folder. Callback supports `leading`, `maxWait`, `cancel`, `flush`           |
| `useThrottledCallback`                       | `src/hooks/useThrottledCallback/`    | 7     | `leading` and `trailing` configurable. `cancel()` clears pending                        |
| `useIntersectionObserver`                    | `src/hooks/useIntersectionObserver/` | 6     | Returns `{ ref, entry, isIntersecting }`. SSR-safe                                      |

**Exports added** (in `src/index.ts`): all five hooks + their option/return types.

**Theme**: `breakpoints`, `BREAKPOINT_ORDER`, `Breakpoint` exported from `entangle-ui`.

`useHotkey` from the original plan was already implemented in v0.8 — confirmed it works as needed for CommandPalette (parses `Cmd+K`, blocks input focus, etc.).

### Phase 2.1 — Card

Located at `src/components/layout/Card/`.

- Variants: `outlined` (default), `filled`, `elevated`
- Sub-components: `Card.Header`, `Card.Media`, `Card.Body`, `Card.Footer`
- Interactive when `onClick` passed (role=button, Enter/Space, focus styles)
- `selected` (accent border tint), `disabled` (dim + pointer-events off)
- 12 tests, exported from `entangle-ui`

### Phase 2.2 — Stat

Located at `src/components/feedback/Stat/`. Kept (not reverted) per session 2 decision.

- Sizes `sm` / `md` / `lg`
- Delta with `direction` (up/down/neutral) + `semantics` (positive/negative/neutral) — flips colors so "errors up = red" works correctly
- Optional leading icon and helper text
- 8 tests, exported from `entangle-ui`

## Done in session 2 (Phase 2 rest + Phase 3)

### Phase 2.3 — Pagination ✅

Located at `src/components/navigation/Pagination/`. Commit `c0d13d3`.

- MUI-style sibling/boundary algorithm in `usePaginationItems` (separate, pure, easy to test)
- 1-based pages, controlled (`page`) + uncontrolled (`defaultPage`) via `useControlledState`
- Optional `showFirstButton` / `showLastButton`, optional `hidePrevButton` / `hideNextButton`
- Three sizes (sm/md/lg), `disabled`, customisable `getItemAriaLabel`
- 17 tests, exported from `entangle-ui`
- Also: fixed pre-existing lint errors in `useIntersectionObserver`

### Phase 3.1 — HoverCard ✅

Located at `src/components/primitives/HoverCard/`. Commit `3c88ce3`.

- Compound API: `HoverCard.Trigger` + `HoverCard.Content`
- Floating UI based (matches `Popover` infrastructure); `useHover` + `useFocus`
- `openDelay` 400ms / `closeDelay` 150ms by default
- Floating UI `safePolygon()` so the cursor can move from trigger to content without closing — toggle off with `disableSafePolygon`
- Portal by default, controlled + uncontrolled, `disabled` flag, opens on keyboard focus
- 14 tests, exported from `entangle-ui`

### Phase 3.2 — Drawer ✅

Located at `src/components/feedback/Drawer/` (per plan). Commit `8d7377b`.

- Compound API: `Drawer.Header` (built-in close button), `Drawer.Body` (scrolls), `Drawer.Footer`, `Drawer.CloseButton`
- Anchors: `left` / `right` / `top` / `bottom` with appropriate slide-in/out animations
- Modal mode (default) renders backdrop + traps focus; non-modal mode leaves the rest of the page interactive
- Sizes `sm` (280) / `md` (360) / `lg` (480) / `xl` (640) plus arbitrary number (px) or string (CSS)
- `closeOnOverlayClick`, `closeOnEscape`, `initialFocusRef`, `portal`, `title` / `description` for ARIA wiring
- Animation lifecycle in `useDrawerAnimation` mirrors `useDialogAnimation`; honors `prefers-reduced-motion: reduce`
- 24 tests, exported from `entangle-ui`

### Phase 3.3 — CommandPalette ✅ (flagship)

Located at `src/components/feedback/CommandPalette/`. Commit `3edf83e`.

- Centred floating dialog (~640 px wide, top 15vh, configurable `width` + `maxHeight`)
- Pure fuzzy matcher in `fuzzySearch.ts` (subsequence + word-boundary + position scoring); also exported as `fuzzyScore` / `fuzzyFilter` from package root
- Search via `useDebouncedValue` (default 150 ms)
- Items support `id`, `label`, `description`, `group`, `icon`, `shortcut` (rendered with `<Kbd>`), `keywords`, `disabled`, per-item `onSelect`
- Custom renderer: `renderItem={(item, { selected, query }) => ReactNode}`
- Recent selections via localStorage with namespaced key (`recentKey`), `maxRecent` cap, graceful fallback when storage is unavailable
- Keyboard: ArrowUp/Down/Home/End to navigate, Enter to select, Escape to close; hover mirrors keyboard selection
- Combobox/listbox roles, `aria-activedescendant`, `aria-controls`, `aria-modal` on the dialog
- Component does NOT bind a global hotkey — consumer wires `useHotkey('Mod+K', open)` (story `WithGlobalHotkey` shows the pattern)
- 22 component tests + 13 fuzzy-search tests = 35 tests, exported from `entangle-ui`

### No virtualization in this cycle

Per session 2 decision: CommandPalette renders all items natively. `@tanstack/react-virtual` is parked for the future Table sprint. Realistic command lists (≤ a few hundred items) render fine without virtualization.

## Done in session 3 (Phase 4 — Form components)

### Phase 4.0 — `useListboxNav` hook ✅

Located at `src/hooks/useListboxNav/`. Commit `e0cd20b`.

- Generic keyboard navigation primitive: `activeIndex`, `next/prev/first/last`, `selectActive`, single `handleKeyDown` covering ArrowUp/ArrowDown/Home/End/Enter/Escape with optional looping.
- Skips disabled items via `isItemDisabled`. Resets active index when the items array changes (reference equality), opt-out via `resetOnItemsChange: false`.
- 19 unit tests (`renderHook`-based).
- Built so MultiSelect and Combobox can share keyboard plumbing instead of duplicating ~150 LOC each.

### Phase 4.1 — TagInput ✅

Located at `src/components/controls/TagInput/`. Commit `ec88ee7`.

- Multi-value text input that turns committed entries into removable chips.
- Controlled + uncontrolled (`value` / `defaultValue` / `onChange`).
- Configurable commit keys (`Enter`, `Comma`, `Space`, `Tab`), optional `addOnBlur`, paste with separators is split into multiple tags.
- Validation pipeline: `normalize` → duplicate / `max` / `validate` callback. Reasons (`'duplicate' | 'max' | 'empty' | 'invalid' | <custom string>`) reported through `onValidate`.
- Custom chip rendering via `renderTag`. Backspace removes the trailing chip when the draft is empty.
- 23 tests (Rendering / Interactions / Validation / Accessibility), exported from `entangle-ui`.

### Phase 4.2 — MultiSelect ✅

Located at `src/components/controls/MultiSelect/`. Commit `8b3cdfa`.

- Multi-value select with inline chips + `+N more` overflow badge once `maxInlineChips` is exceeded.
- Flat or grouped options, optional searchable mode, controlled + uncontrolled, configurable `max`, optional `clearable`, `closeOnSelect`.
- Keyboard navigation through `useListboxNav`. Listbox has `aria-multiselectable="true"`.
- Three sizes, three variants, full label / helper / error wiring, hidden form-input via `name`.
- 18 tests, exported from `entangle-ui`.

### Phase 4.3 — Combobox ✅

Located at `src/components/controls/Combobox/`. Commit `39e9986`.

- Single-value select with editable input. Filters options as the user types using `fuzzyScore` reused from CommandPalette; consumers can swap a custom `filterFn` returning either a boolean or a numeric score.
- Modes: default (must match an option), `freeSolo` (accept any typed value), `creatable` (offers a `Create "<query>"` row, fires `onCreate`).
- `loading` state with spinner row, `openOnFocus` for browseable menu use, optional `clearable`.
- ARIA combobox pattern with `aria-activedescendant` and shared listbox keyboard from `useListboxNav`.
- 17 tests, exported from `entangle-ui`.

### Phase 4.4 — FileUploader ✅

Located at `src/components/controls/FileUploader/`. Commit `c869f9c`.

- Drag-and-drop dropzone with click-to-browse fallback. Per-file row shows name, size, status badge (`pending` / `uploading` / `done` / `error`), and an animated progress bar.
- `accept` matches MIME types (`image/*`) and extensions (`.pdf`); also enforces `minSize`, `maxSize`, `maxFiles`. Custom sync `validate` callback. Rejections surface via `onReject(file, reason)`.
- Presentational: the component does not perform the upload. The consumer drives the actual request from `onFilesAdd` and reflects progress back through a controlled `value`. This keeps it framework-agnostic.
- Custom row rendering via `renderItem`.
- 13 tests, exported from `entangle-ui`.

### After session 3

- 2268 tests passing (was 2178 after session 2). +90 tests across the four components and the new hook.
- Lint clean, `tsc --noEmit` clean, format clean.
- All four Phase 4 components and `useListboxNav` exported from `entangle-ui` and listed in the public type bag.
- Each component shipped with its own changeset in `.changeset/`.

## Remaining work for v0.9

Phase 4 is complete. Decide whether to mark PR #70 ready or leave it as a draft for review (this session left it as a draft).

### Out of v0.9 (per user decision)

- Table/DataGrid (T-17) — own sprint, will use `@tanstack/react-virtual`
- T-18/19/20 (CI infrastructure) — separate later

## Conventions confirmed across sessions

- `BaseComponent<HTMLDivElement>` from `@/types/common`
- `Prettify<T>` for exported props types
- `Omit<BaseComponent<...>, 'title'>` whenever the component takes a `title: ReactNode` prop (clashes with HTMLAttributes); same trick for `'onSelect'` (CommandPalette) and `'children'` (Drawer.CloseButton)
- Vanilla Extract `recipe` for variant logic; `createVar` + `assignInlineVars` for runtime values (e.g., per-instance colors)
- Compound components with `Object.assign(Root, { Sub1, Sub2 })` (Card, HoverCard, Drawer)
- For unions accepting an open string with autocomplete on a few presets: `'sm' | 'md' | 'lg' | 'xl' | (string & {}) | number` — the bare `string` was being collapsed by ESLint's `no-redundant-type-constituents` rule
- `'use client'` directive at top of every component file
- `displayName` set explicitly on every component (including compound sub-components like `Drawer.Header`)
- `/*#__PURE__*/` annotations on `React.memo` and `createContext` calls
- Tests: `renderWithTheme` from `@/tests/testUtils`; describe blocks Rendering / Interactions / Accessibility (or domain-specific blocks like `Filtering`, `Keyboard navigation`)
- For Floating UI hover tests: fire `mouseenter`/`mouseleave` (not `pointerenter`/`pointerleave`) — `useHover` listens directly to mouse events on the reference element
- `cx` from `@/utils/cx` for class merging
- Stories: `Meta`/`StoryObj` from `@storybook/react`; one `Default` + variant stories
- After each component: `npm run lint` clean, `npx tsc --noEmit` clean, `npx vitest run` clean, then changeset (`npx changeset`), then per-component commit
- No non-null assertions in tests — replace `instances[0]!` with explicit `if (!x) throw new Error(...)` to satisfy `@typescript-eslint/no-non-null-assertion`

## Pre-flight checklist for the next session

```bash
# From the entangle-ui repo root
git fetch origin
git checkout claude/entangle-ui-v0.9-release-iT5GO
git pull origin claude/entangle-ui-v0.9-release-iT5GO

# Confirm baseline
npx tsc --noEmit
npx vitest run
```

After session 2: 2178 tests passing, lint clean, type clean. Branch ready for review.

## Decisions parked

- **PR #70 readiness**: left as draft after session 3. Flip to ready once the user has reviewed the four new components.
- **Stat**: kept (not reverted) per session 2 decision.

---
'entangle-ui': patch
---

Add experimental Recraft V4.1-generated icons under the `ExperimentalIcons` namespace (`RobotIcon`, `FolderCogIcon`), normalized to the `Icon` contract (24x24 viewBox, `currentColor`, merged `fill-rule="evenodd"` path). Includes a conversion script (`scripts/recraft-to-experimental-icon.mjs`) and a docs preview page comparing them to the current line icons. Not part of the stable API.

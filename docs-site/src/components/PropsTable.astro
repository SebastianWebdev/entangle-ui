---
interface Prop {
  name: string;
  type: string;
  default?: string;
  description: string;
  required?: boolean;
}

interface Props {
  props: Prop[];
}

const { props } = Astro.props;
---

<div class="props-table-wrapper">
  <table class="props-table">
    <thead>
      <tr>
        <th>Prop</th>
        <th>Type</th>
        <th>Default</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      {props.map((prop: Prop) => (
        <tr>
          <td>
            <code>{prop.name}</code>
            {prop.required && <span class="required" title="Required">*</span>}
          </td>
          <td><code>{prop.type}</code></td>
          <td>{prop.default ? <code>{prop.default}</code> : <span class="no-default">—</span>}</td>
          <td>{prop.description}</td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

<style>
  .props-table-wrapper {
    overflow-x: auto;
    margin: 1rem 0;
  }

  .props-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.875rem;
  }

  .props-table th {
    text-align: left;
    padding: 0.625rem 0.75rem;
    border-bottom: 2px solid var(--sl-color-gray-5);
    color: var(--sl-color-white);
    font-weight: 600;
  }

  .props-table td {
    padding: 0.5rem 0.75rem;
    border-bottom: 1px solid var(--sl-color-gray-5);
    vertical-align: top;
  }

  .props-table code {
    font-size: 0.8125rem;
    padding: 0.125rem 0.375rem;
    border-radius: 3px;
    background: var(--sl-color-gray-6);
    color: var(--sl-color-accent-high);
    white-space: nowrap;
  }

  .props-table td:last-child {
    color: var(--sl-color-gray-2);
  }

  .required {
    color: var(--sl-color-accent);
    margin-left: 0.25rem;
    font-weight: bold;
  }

  .no-default {
    color: var(--sl-color-gray-4);
  }
</style>

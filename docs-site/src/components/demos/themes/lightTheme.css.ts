import { createLightTheme } from '@/theme/createLightTheme';

/**
 * Build-time generated light-theme class used by the demos in
 * `LightThemeDemo.tsx`. Apply via `VanillaThemeProvider` or a plain
 * `<div className={lightThemeClass}>` wrapper.
 */
export const lightThemeClass = createLightTheme();

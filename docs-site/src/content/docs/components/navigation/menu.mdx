---
title: Menu
description: Composable menu with icon/label/shortcut items, radio and checkbox selection, grouped items, nested submenus, and keyboard navigation.
---

import PropsTable from '../../../../components/PropsTable.astro';
import ComponentPreview from '../../../../components/ComponentPreview.astro';
import MenuDemo, {
  MenuWithIcons,
  MenuGroups,
  MenuRadioSelection,
  MenuCheckboxSelection,
  MenuEndContent,
  MenuSubmenus,
  MenuCustomTrigger,
  MenuDisabled,
} from '../../../../components/demos/navigation/MenuDemo';

Composable menu component for editor interfaces. Build menus by composing
`Menu.Trigger`, `Menu.Content`, and item primitives — no configuration object.
Each item lays out as **icon (left) · label (center) · shortcut/action
(right)**. Built on top of `@base-ui/react` Menu primitives with full keyboard
navigation support.

<ComponentPreview title="Live Preview">
  <MenuDemo client:only="react" />
</ComponentPreview>

## Import

```tsx
import { Menu } from 'entangle-ui';
```

## Usage

```tsx
<Menu>
  <Menu.Trigger>Options</Menu.Trigger>
  <Menu.Content>
    <Menu.Item onClick={handleCopy}>Copy</Menu.Item>
    <Menu.Item onClick={handlePaste}>Paste</Menu.Item>
    <Menu.Item onClick={handleDelete}>Delete</Menu.Item>
  </Menu.Content>
</Menu>
```

`Menu.Trigger` renders the library `Button` by default. Pass `render` to use a
different element (for example an `IconButton`):

```tsx
<Menu.Trigger
  render={
    <IconButton aria-label="More actions">
      <DotsVerticalIcon size="sm" />
    </IconButton>
  }
/>
```

<ComponentPreview title="Custom trigger">
  <MenuCustomTrigger client:only="react" />
</ComponentPreview>

## Item Anatomy

Every item exposes three slots. `icon` sits on the left, the children are the
label, and `shortcut` / `endContent` sit on the right.

```tsx
<Menu.Item
  icon={<SaveIcon />}
  shortcut="⌘S"
  endContent={<Badge>3</Badge>}
  onClick={handleSave}
>
  Save
</Menu.Item>
```

<ComponentPreview title="Icons & shortcuts">
  <MenuWithIcons client:only="react" />
</ComponentPreview>

`endContent` accepts any node — a badge, a `Switch`, etc. — rendered on the
right of the row.

<ComponentPreview title="Right-side actions (endContent)">
  <MenuEndContent client:only="react" />
</ComponentPreview>

## Groups and Separators

Wrap related items in `Menu.Group` for an optional label, and split sections
with `Menu.Separator`.

```tsx
<Menu.Content>
  <Menu.Group label="File">
    <Menu.Item shortcut="⌘N">New File</Menu.Item>
    <Menu.Item shortcut="⌘S">Save</Menu.Item>
  </Menu.Group>
  <Menu.Separator />
  <Menu.Group label="Edit">
    <Menu.Item shortcut="⌘Z">Undo</Menu.Item>
    <Menu.Item shortcut="⇧⌘Z">Redo</Menu.Item>
  </Menu.Group>
</Menu.Content>
```

<ComponentPreview title="Groups & separators">
  <MenuGroups client:only="react" />
</ComponentPreview>

## Radio Selection

Single selection within a group. `Menu.RadioGroup` owns the selected value and
each `Menu.RadioItem` shows an indicator when active.

```tsx
const [view, setView] = useState('perspective');

<Menu.Content>
  <Menu.RadioGroup value={view} onValueChange={setView}>
    <Menu.RadioItem value="perspective">Perspective</Menu.RadioItem>
    <Menu.RadioItem value="orthographic">Orthographic</Menu.RadioItem>
    <Menu.RadioItem value="front">Front</Menu.RadioItem>
  </Menu.RadioGroup>
</Menu.Content>;
```

<ComponentPreview title="Radio selection">
  <MenuRadioSelection client:only="react" />
</ComponentPreview>

## Checkbox Selection

Each `Menu.CheckboxItem` owns its own checked state.

```tsx
const [grid, setGrid] = useState(true);
const [wireframe, setWireframe] = useState(false);

<Menu.Content>
  <Menu.Group label="Overlays">
    <Menu.CheckboxItem checked={grid} onCheckedChange={setGrid}>
      Grid
    </Menu.CheckboxItem>
    <Menu.CheckboxItem checked={wireframe} onCheckedChange={setWireframe}>
      Wireframe
    </Menu.CheckboxItem>
  </Menu.Group>
</Menu.Content>;
```

<ComponentPreview title="Checkbox selection">
  <MenuCheckboxSelection client:only="react" />
</ComponentPreview>

## Nested Submenus

Compose `Menu.Sub`, `Menu.SubTrigger`, and `Menu.SubContent`. The submenu
trigger renders a chevron automatically.

```tsx
<Menu.Content>
  <Menu.Sub>
    <Menu.SubTrigger icon={<TransformIcon />}>Transform</Menu.SubTrigger>
    <Menu.SubContent>
      <Menu.Item onClick={handleMove}>Move</Menu.Item>
      <Menu.Item onClick={handleRotate}>Rotate</Menu.Item>
      <Menu.Item onClick={handleScale}>Scale</Menu.Item>
    </Menu.SubContent>
  </Menu.Sub>
</Menu.Content>
```

<ComponentPreview title="Nested submenus">
  <MenuSubmenus client:only="react" />
</ComponentPreview>

## Disabled Items

```tsx
<Menu.Item disabled>Paste</Menu.Item>
```

<ComponentPreview title="Disabled item">
  <MenuDisabled client:only="react" />
</ComponentPreview>

## Custom Selection Indicators

Override the default radio/checkbox indicator per item via `indicator`.

```tsx
<Menu.RadioItem value="solid" indicator={<DotIcon />}>
  Solid
</Menu.RadioItem>
<Menu.CheckboxItem checked indicator={<TickIcon />}>
  Snapping
</Menu.CheckboxItem>
```

## API

### Menu

The root. Owns open/close state.

<PropsTable
  props={[
    {
      name: 'children',
      type: 'ReactNode',
      description: 'Trigger and content of the menu.',
    },
    {
      name: 'open',
      type: 'boolean',
      description: 'Controlled open state.',
    },
    {
      name: 'defaultOpen',
      type: 'boolean',
      description: 'Uncontrolled initial open state.',
    },
    {
      name: 'onOpenChange',
      type: '(open: boolean) => void',
      description: 'Called when the menu opens or closes.',
    },
    {
      name: 'modal',
      type: 'boolean',
      default: 'true',
      description: 'Whether the menu traps interaction while open.',
    },
    {
      name: 'disabled',
      type: 'boolean',
      default: 'false',
      description: 'Disables opening the menu.',
    },
  ]}
/>

### Menu.Trigger

<PropsTable
  props={[
    {
      name: 'children',
      type: 'ReactNode',
      description: 'Trigger content.',
    },
    {
      name: 'render',
      type: 'ReactElement',
      default: '<Button />',
      description: 'Replace the default Button with a custom element.',
    },
    {
      name: 'openOnHover',
      type: 'boolean',
      default: 'false',
      description: 'Also open the menu when the trigger is hovered.',
    },
    {
      name: 'disabled',
      type: 'boolean',
      default: 'false',
      description: 'Disables the trigger.',
    },
  ]}
/>

### Menu.Content

The positioned popup surface. Place items or any custom node inside.

<PropsTable
  props={[
    {
      name: 'children',
      type: 'ReactNode',
      description: 'Items, groups, or custom panel content.',
    },
    {
      name: 'side',
      type: "'top' | 'right' | 'bottom' | 'left'",
      description: 'Preferred side relative to the trigger.',
    },
    {
      name: 'align',
      type: "'start' | 'center' | 'end'",
      description: 'Alignment along the chosen side.',
    },
    {
      name: 'sideOffset',
      type: 'number',
      default: '4',
      description: 'Gap in px between trigger and popup.',
    },
    {
      name: 'alignOffset',
      type: 'number',
      description: 'Offset in px along the alignment axis.',
    },
  ]}
/>

### Menu.Item

<PropsTable
  props={[
    {
      name: 'children',
      type: 'ReactNode',
      description: 'Label content (center slot).',
    },
    {
      name: 'icon',
      type: 'ReactNode',
      description: 'Icon rendered in the left slot.',
    },
    {
      name: 'shortcut',
      type: 'ReactNode',
      description: 'Keyboard shortcut hint rendered on the right.',
    },
    {
      name: 'endContent',
      type: 'ReactNode',
      description:
        'Arbitrary node rendered on the right (badge, switch, etc.).',
    },
    {
      name: 'onClick',
      type: '(event: MouseEvent) => void',
      description: 'Click handler.',
    },
    {
      name: 'disabled',
      type: 'boolean',
      default: 'false',
      description: 'Disables the item.',
    },
    {
      name: 'closeOnClick',
      type: 'boolean',
      default: 'true',
      description: 'Whether clicking the item closes the menu.',
    },
  ]}
/>

### Menu.RadioGroup / Menu.RadioItem

`RadioGroup` accepts `value`, `defaultValue`, and `onValueChange(value)`.
`RadioItem` requires a `value` and accepts the same `icon`-less slot props as
`Menu.Item` plus `indicator`.

### Menu.CheckboxItem

Accepts `checked`, `defaultChecked`, `onCheckedChange(checked)`, `indicator`,
`shortcut`, `endContent`, and `disabled`.

### Menu.Group

Accepts an optional `label` plus children.

### Menu.Sub / Menu.SubTrigger / Menu.SubContent

`Sub` groups the submenu. `SubTrigger` accepts `icon`, `disabled`, and children
(a chevron is added automatically). `SubContent` is the submenu popup and takes
the same props as `Menu.Content`.

## Accessibility

- Built on `@base-ui/react` Menu primitives with the WAI-ARIA menu pattern
- Full keyboard navigation: Arrow Up/Down to move, Enter to activate, Escape to close
- Radio groups use proper radio role semantics
- Group labels are exposed to screen readers
- Disabled items are excluded from keyboard navigation
- Focus is returned to the trigger when the menu closes

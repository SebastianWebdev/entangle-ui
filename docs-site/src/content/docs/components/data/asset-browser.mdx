---
title: AssetBrowser
description: Controlled content browser for files and folders — grid/list views, thumbnails, folder navigation, search, filter, sort, selection, and drag-and-drop.
---

import PropsTable from '../../../../components/PropsTable.astro';
import ComponentPreview from '../../../../components/ComponentPreview.astro';
import AssetBrowserDemo, {
  AssetBrowserGridView,
  AssetBrowserListView,
  AssetBrowserStates,
} from '../../../../components/demos/data/AssetBrowserDemo';

The content browser of a professional editor — the surface that lists assets (textures, models, materials, audio, arbitrary files) and the folders that hold them, like the Unreal Content Browser or the Unity Project window. It composes the existing `DataTable` (list view), `Card` grid (grid view), `TreeView` (folder sidebar), and `Breadcrumbs` (path bar).

AssetBrowser is **controlled and presentational**: it does not own a filesystem. You feed it the contents of the _current folder_ plus a _folder tree_, and it reports navigation, selection, and drag intent through callbacks. Lazy loading and any backend (local FS, S3, an in-memory catalog) work out of the box.

<ComponentPreview title="Live Preview">
  <AssetBrowserDemo client:only="react" />
</ComponentPreview>

## Import

```tsx
import { AssetBrowser } from 'entangle-ui';
import type { AssetItem, AssetPathSegment, AssetSortState } from 'entangle-ui';
```

## Usage

Pass the current folder's `items`, the `path` for the breadcrumb bar, and a `folderTree` for the sidebar. Respond to `onNavigate` by swapping `items` for the entered folder — AssetBrowser never walks a tree itself.

```tsx
const [folder, setFolder] = useState('root');
const [selection, setSelection] = useState<string[]>([]);

<AssetBrowser
  items={contentsOf(folder)}
  path={pathTo(folder)}
  folderTree={tree}
  currentFolderId={folder}
  selection={selection}
  onSelectionChange={ids => setSelection(ids)}
  onNavigate={id => {
    setFolder(id);
    setSelection([]);
  }}
  onItemOpen={item => openAsset(item)}
/>;
```

Each entry is an `AssetItem` discriminated by `kind` (`'file'` or `'folder'`). Activating a folder (double-click / Enter) fires `onNavigate`; activating a file fires `onItemOpen`.

```tsx
const items: AssetItem[] = [
  { id: 'textures', name: 'Textures', kind: 'folder' },
  {
    id: 'wood',
    name: 'wood.png',
    kind: 'file',
    assetType: 'image',
    size: 204800,
    thumbnailUrl: '/thumbs/wood.png',
  },
];
```

## Views

Toggle between a thumbnail **grid** and a **list** with the toolbar control (or the `view` / `defaultView` props). The grid windows its rows so thousands of thumbnails stay smooth; the list reuses `DataTable`'s virtualization. Selection is preserved when you switch.

<ComponentPreview title="Grid view">
  <AssetBrowserGridView client:only="react" />
</ComponentPreview>

<ComponentPreview title="List view">
  <AssetBrowserListView client:only="react" />
</ComponentPreview>

## Thumbnails

Resolution order is `renderThumbnail` → `thumbnailUrl` (lazy-loaded) → `item.icon` → a type-derived fallback icon. Use `renderThumbnail` for canvas-rendered previews (e.g. a 3D model preview):

```tsx
<AssetBrowser
  items={items}
  renderThumbnail={(item, { size }) => (
    <ModelPreview src={item.meta?.url} px={size} />
  )}
/>
```

## Selection

`selectionMode` is `'multiple'` by default (`'single'` or `false` also supported). Click replaces, Ctrl/Cmd-click toggles, Shift-click range-selects, and dragging in empty grid space draws a marquee. `onSelectionChange` reports the new ids plus a `reason`.

## Drag & drop

Provide `onItemsMove` to enable dropping items onto folders, and `onFilesImport` to accept OS files dropped onto the surface. Items are also draggable out with a configurable `dataTransfer` payload (`dragOutMimeType`). The actual move/import mutation is yours — AssetBrowser only reports intent.

```tsx
<AssetBrowser
  items={items}
  onItemsMove={({ itemIds, targetFolderId }) => move(itemIds, targetFolderId)}
  onFilesImport={({ files, targetFolderId }) => upload(files, targetFolderId)}
/>
```

## States

Set `loading` for a loading placeholder, or pass `emptyState` to override the default empty message shown when there are no items.

<ComponentPreview title="Empty">
  <AssetBrowserStates client:only="react" />
</ComponentPreview>

## Accessibility

- Grid view is a `role="grid"` with `aria-multiselectable`; cells are `role="gridcell"` with `aria-selected` and a roving tabindex.
- Arrow keys move focus in 2D; Home/End jump to the first/last item; PageUp/PageDown page by a viewport; Enter opens; Space toggles selection; Ctrl/Cmd+A selects all; Escape clears.
- The breadcrumb bar is a labelled `nav`; the current segment is `aria-current="page"`.
- A visually-hidden live region announces the item count and selection size.

## API Reference

<PropsTable
  props={[
    {
      name: 'items',
      type: 'AssetItem[]',
      required: true,
      description:
        'Direct children of the current folder (files + sub-folders).',
    },
    {
      name: 'path',
      type: 'AssetPathSegment[]',
      description:
        'Current location, oldest → newest. Drives the breadcrumb bar.',
    },
    {
      name: 'folderTree',
      type: 'TreeNodeData[]',
      description:
        'Folder hierarchy for the sidebar. Omit to hide the sidebar.',
    },
    {
      name: 'currentFolderId',
      type: 'string',
      description:
        'Id of the folder currently shown. Highlights the sidebar node.',
    },
    {
      name: 'view',
      type: "'grid' | 'list'",
      description: 'Controlled view mode.',
    },
    {
      name: 'defaultView',
      type: "'grid' | 'list'",
      default: "'grid'",
      description: 'Uncontrolled initial view.',
    },
    {
      name: 'onViewChange',
      type: '(view) => void',
      description: 'Fired when the view toggles.',
    },
    {
      name: 'thumbnailSize',
      type: "'sm' | 'md' | 'lg' | number",
      default: "'md'",
      description: 'Grid thumbnail scale (number = px).',
    },
    {
      name: 'density',
      type: "'comfortable' | 'compact' | 'dense'",
      default: "'comfortable'",
      description: 'List-view row density.',
    },
    {
      name: 'selectionMode',
      type: "'single' | 'multiple' | false",
      default: "'multiple'",
      description: 'Selection behaviour.',
    },
    {
      name: 'selection',
      type: 'string[]',
      description: 'Controlled selected ids.',
    },
    {
      name: 'defaultSelection',
      type: 'string[]',
      description: 'Uncontrolled initial selection.',
    },
    {
      name: 'onSelectionChange',
      type: '(ids, { reason }) => void',
      description: 'Fired when selection changes.',
    },
    { name: 'search', type: 'string', description: 'Controlled search query.' },
    {
      name: 'onSearchChange',
      type: '(query) => void',
      description: 'Fired when the search input changes.',
    },
    {
      name: 'manualSearch',
      type: 'boolean',
      default: 'false',
      description: 'When true, you pre-filter items by name.',
    },
    {
      name: 'filters',
      type: '{ types?: string[] }',
      description: 'Controlled type filters.',
    },
    {
      name: 'onFiltersChange',
      type: '(filters) => void',
      description: 'Fired when filters change.',
    },
    {
      name: 'sort',
      type: '{ field, direction }',
      description: 'Controlled sort state.',
    },
    {
      name: 'onSortChange',
      type: '(sort) => void',
      description: 'Fired when sort changes.',
    },
    {
      name: 'onNavigate',
      type: '(folderId, source) => void',
      description: 'Fired when the user enters a folder.',
    },
    {
      name: 'onItemOpen',
      type: '(item) => void',
      description: 'Fired when a file is activated (double-click / Enter).',
    },
    {
      name: 'columns',
      type: 'DataTableColumn<AssetItem>[]',
      description: 'Override the default list columns.',
    },
    {
      name: 'onItemsMove',
      type: '({ itemIds, targetFolderId }) => void',
      description: 'Enables dropping items onto folders.',
    },
    {
      name: 'onFilesImport',
      type: '({ files, targetFolderId }) => void',
      description: 'Enables external OS-file drop.',
    },
    {
      name: 'renderThumbnail',
      type: '(item, ctx) => ReactNode',
      description: 'Custom thumbnail (e.g. canvas preview).',
    },
    {
      name: 'renderItem',
      type: '(item, state) => ReactNode',
      description: "Fully replace a grid cell's content.",
    },
    {
      name: 'renderItemContextMenu',
      type: '(items) => ReactNode',
      description: 'Body of the item context menu.',
    },
    {
      name: 'loading',
      type: 'boolean',
      default: 'false',
      description: 'Show a loading placeholder.',
    },
    {
      name: 'emptyState',
      type: 'ReactNode',
      description: 'Override the default empty view.',
    },
    {
      name: 'virtualized',
      type: "boolean | 'auto'",
      default: "'auto'",
      description: 'Grid windowing; engages above the threshold.',
    },
    {
      name: 'virtualizationThreshold',
      type: 'number',
      default: '200',
      description: 'Item count above which auto windowing engages.',
    },
    {
      name: 'showStatusBar',
      type: 'boolean',
      default: 'false',
      description: 'Show the count/selection footer + thumbnail-size control.',
    },
  ]}
/>
